---
import Layout from '@/layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('news'))
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime())
  .slice(0, 3);
---

<Layout title="Офіс НГА - Офіс Незалежного Громадського Аудиту">
  <!-- Hero Section -->
  <section class="relative bg-white py-20 sm:py-32">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="mx-auto max-w-3xl text-center">
        <div class="accent-bar-left mb-8 inline-block">
          <h1 class="text-3xl font-bold text-gray-900 sm:text-5xl lg:text-6xl leading-tight">
            Офіс Незалежного Громадського Аудиту
          </h1>
        </div>
        <p class="mt-6 text-lg sm:text-xl text-gray-600 leading-relaxed">
          Повертаємо владу під Твій контроль.
        </p>
        <div class="mt-12 flex flex-col gap-4 sm:flex-row sm:justify-center">
          <a href="/news" class="btn-primary inline-block text-center">
            Читати Новини
          </a>
          <a href="/#about" class="btn-secondary inline-block text-center">
            Дізнатися Більше
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Latest Posts Preview -->
  <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-20 border-t border-gray-200">
    <div class="mb-12">
      <h2 class="text-4xl font-bold text-gray-900 accent-bar-left">Останні Аудити</h2>
      <p class="mt-4 text-lg text-gray-600">Актуальні звіти про проведені розслідування</p>
    </div>
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {
        posts.map((post) => (
          <a href={`/news/${post.slug}`} class="group card">
            <div class="mb-4 h-40 overflow-hidden rounded-lg bg-gray-200">
              {post.data.image && (
                <img
                  src={post.data.image}
                  alt={post.data.title}
                  class="h-full w-full object-cover transition-transform group-hover:scale-105"
                />
              )}
            </div>
            <p class="text-sm font-semibold text-blue-700">
              {new Date(post.data.pubDate).toLocaleDateString('uk-UA', {
                year: 'numeric',
                month: 'long',
                day: 'numeric',
              })}
            </p>
            <h3 class="mt-3 text-xl font-bold text-gray-900 group-hover:text-blue-700 transition-colors">
              {post.data.title}
            </h3>
            <p class="mt-2 line-clamp-2 text-gray-600">{post.data.description}</p>
            <div class="mt-4 flex items-center gap-2 text-sm font-semibold text-blue-700">
              Читати далі →
            </div>
          </a>
        ))
      }
    </div>
    <div class="mt-12 text-center">
      <a href="/news" class="btn-primary">Переглянути Всі Публікації</a>
    </div>
  </section>

  <!-- About Section -->
  <section id="about" class="border-t border-gray-200 bg-white py-20">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid gap-12 lg:grid-cols-2 lg:gap-8">
        <div>
          <h2 class="text-4xl font-bold text-gray-900 accent-bar-left">Про Організацію</h2>
          <p class="mt-4 text-lg text-gray-600 leading-relaxed">
            Офіс НГА - це громадська організація, яка спеціалізується на виявленні та документуванні фактів корупції та неефективного використання бюджетних коштів.
          </p>
          <div class="mt-8 space-y-4">
            <div class="flex gap-4">
              <div class="flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-lg bg-blue-100">
                <span class="text-xl">🔍</span>
              </div>
              <div>
                <h3 class="font-bold text-gray-900">Незалежні Розслідування</h3>
                <p class="mt-1 text-gray-600">Проводимо глибокі аудити без впливу політичних сил</p>
              </div>
            </div>
            <div class="flex gap-4">
              <div class="flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-lg bg-yellow-100">
                <span class="text-xl">📊</span>
              </div>
              <div>
                <h3 class="font-bold text-gray-900">Прозорість</h3>
                <p class="mt-1 text-gray-600">Публікуємо всі звіти та висновки для громадськості</p>
              </div>
            </div>
            <div class="flex gap-4">
              <div class="flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-lg bg-blue-100">
                <span class="text-xl">🤝</span>
              </div>
              <div>
                <h3 class="font-bold text-gray-900">Громадська Участь</h3>
                <p class="mt-1 text-gray-600">Залучаємо громадськість до контролю витрат держави</p>
              </div>
            </div>
          </div>
        </div>
        <div class="flex items-center justify-center rounded-lg border border-gray-200 bg-gray-50 p-12">
          <div class="text-center">
            <p class="text-6xl font-bold text-gray-900">2</p>
            <p class="mt-4 text-xl text-gray-600">Проведених Аудитів</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section id="donate" class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-20">
    <div class="rounded-lg border-l-4 border-blue-700 bg-white p-12 text-center shadow-lg">
      <h2 class="text-4xl font-bold text-gray-900">Підтримайте Нашу Роботу</h2>
      <p class="mt-4 text-xl text-gray-600 leading-relaxed">
        Ваша підтримка дає нам можливість продовжувати боротьбу з корупцією
      </p>
      <button class="mt-8 rounded-lg bg-blue-700 px-8 py-3 font-bold text-white hover:bg-blue-800 transition-colors">
        Зробити Пожертву
      </button>
    </div>
  </section>
</Layout>
